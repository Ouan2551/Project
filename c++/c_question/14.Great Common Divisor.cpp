#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int n1, n2; cin >> n1 >> n2;
    for (int i = 1; i < n1 && i < n2; i++)
    {
        
    }
    return 0;
}