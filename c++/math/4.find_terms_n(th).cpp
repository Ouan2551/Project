#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    string term = ""; getline(cin, term); int m = term.size();
    vector<int> numbers;
    return 0;
}